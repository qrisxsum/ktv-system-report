# KTV 多店经营分析系统 (BI Lite) 项目实施方案

## 文档信息

| 项目 | 内容 |
|------|------|
| **项目名称** | KTV 多店经营分析系统 (BI Lite) |
| **版本号** | v1.0 |
| **创建日期** | 2025-12-09 |
| **核心目标** | 上传 Excel → 清洗入库 → 老板看图 |

---

## 一、项目概述

### 1.1 业务背景

KTV 连锁门店日常经营中会产生三类核心数据报表：
1. **包厢开台分析表** - 记录每个包厢的开台/关房时间、消费金额、支付方式等
2. **酒水销售分析表** - 记录各类商品的销售数量、金额、成本、利润等
3. **预订汇总表** - 记录各员工（销售经理、服务员、收银员）的订台业绩和收款明细

### 1.2 核心痛点

- 各门店数据分散，老板无法快速了解整体经营状况
- Excel 数据格式复杂，人工汇总费时费力
- 缺乏可视化分析工具，难以发现经营问题

### 1.3 解决方案

构建一套轻量级 BI 系统：
- **数据上传**：各店店长上传 CSV/Excel 文件
- **自动清洗**：系统自动识别文件类型、清洗脏数据、入库存储
- **可视化展示**：老板登录系统查看图表和关键指标

---

## 二、数据源分析

### 2.1 包厢开台分析表结构

**文件特征**：
- 第1行：标题行（包含日期范围和门店名称）
- 第2行：主字段名
- 第3行：支付方式子字段
- 最后一行：合计行

**核心字段**（共58列）：

| 列序 | 原始字段名 | 字段含义 | 数据类型 | 示例值 |
|-----|-----------|---------|---------|--------|
| 1 | 包厢名称 | 包厢编号 | VARCHAR(20) | K07, KKK, JJJ |
| 2 | 包厢类型 | 包厢规格 | VARCHAR(20) | 电音中包、电音小包、派对包 |
| 3 | 区域名称 | 所属区域 | VARCHAR(20) | KTV区、派对包 |
| 4 | 开台单号 | 业务单号 | VARCHAR(30) | Z-KT25120200041 |
| 5 | 开房计费模式 | 套餐类型 | VARCHAR(100) | 线上午夜档5H纯唱... |
| 6 | 开房时间 | 开台时间 | DATETIME | 2025-12-03 00:37:29 |
| 7 | 关房时间 | 关台时间 | DATETIME | 2025-12-03 04:54:48 |
| 8 | 清洁时间 | 清洁完成时间 | DATETIME | - |
| 9 | 营业日 | 归属营业日 | DATE | 2025-12-02 |
| 10 | 订房人 | 预订人员 | VARCHAR(50) | - |
| 11 | 订房人部门 | 预订人部门 | VARCHAR(50) | - |
| 15 | 消费时长 | 消费分钟数 | INT | 257 |
| 16 | 时段 | 营业时段 | VARCHAR(20) | 黄金档、白天档、整档 |
| 20 | 账单合计 | 总消费金额 | DECIMAL(12,2) | 225 |
| 21 | 应收金额 | 应收金额 | DECIMAL(12,2) | 225 |
| 22 | 实收金额 | 实收金额 | DECIMAL(12,2) | 225 |
| 23 | 赠送 | 赠送金额 | DECIMAL(12,2) | 0 |
| 24 | 抹零金额 | 抹零金额 | DECIMAL(12,2) | 0 |
| 30 | 基本业绩 | 基础业绩 | DECIMAL(12,2) | 225 |
| 31 | 低消费 | 低消金额 | DECIMAL(12,2) | 0 |
| 32 | 低消差额 | 低消差额 | DECIMAL(12,2) | 0 |
| 33 | 计入低消金额 | 计入低消 | DECIMAL(12,2) | 56 |

**支付方式字段**（第37-58列）：

| 原始字段名 | 分类 | 说明 |
|-----------|------|------|
| 微信支付 | 收入类 | 实收渠道 |
| 支付宝 | 收入类 | 实收渠道 |
| 会员支付 | 权益类 | 会员卡总支付 |
| 会员本金 | 权益类-本金 | 会员充值本金 |
| 会员赠送 | 权益类-赠送 | 会员充值赠送 |
| 现金 | 收入类 | 实收渠道 |
| 银行卡 | 收入类 | 实收渠道 |
| 付呗 | 收入类 | 实收渠道 |
| 店长签单 | 权益类 | 内部签单 |
| 美团 | 收入类 | 团购渠道 |
| 抖音 | 收入类 | 团购渠道 |
| 招待 | 权益类 | 内部招待 |
| 定金消费 | 收入类 | 定金抵扣 |
| 高德 | 收入类 | 团购渠道 |

---

### 2.2 酒水销售分析表结构

**文件特征**：
- 第1行：标题行（包含日期范围和门店名称）
- 第2-3行：多级表头
- 最后一行：合计行

**核心字段**（共25列）：

| 列序 | 原始字段名 | 字段含义 | 数据类型 | 示例值 |
|-----|-----------|---------|---------|--------|
| 1 | 酒水名称 | 商品名称 | VARCHAR(100) | 青岛崂山、什锦果盘 |
| 2 | 类别名称 | 商品分类 | VARCHAR(50) | 啤酒、群果荟萃、卤味小吃 |
| 3 | 单位 | 计量单位 | VARCHAR(10) | 瓶、份、听、壶 |
| 4 | 区域 | 销售区域 | VARCHAR(20) | KTV区、派对包 |
| 5 | 成本-小计 | 总成本 | DECIMAL(12,4) | 746.6667 |
| 6 | 成本-销售 | 销售成本 | DECIMAL(12,4) | 718.6667 |
| 7 | 成本-赠送 | 赠送成本 | DECIMAL(12,4) | 28 |
| 8 | 利润 | 毛利润 | DECIMAL(12,4) | 5018.8922 |
| 9 | 利润率 | 毛利率(%) | DECIMAL(8,2) | 672.17 |
| 10 | 合计-数量 | 总数量 | INT | 320 |
| 11 | 合计-金额 | 总金额 | DECIMAL(12,4) | 6029.5589 |
| 12 | 销售数量-小计 | 销售数量合计 | INT | 308 |
| 13 | 销售数量-销售 | 纯销售数量 | INT | 32 |
| 14 | 销售数量-套餐子物品 | 套餐内数量 | INT | 276 |
| 16 | 销售金额-小计 | 销售金额合计 | DECIMAL(12,4) | 5765.5589 |
| 17 | 销售金额-销售 | 纯销售金额 | DECIMAL(12,4) | 0 |
| 18 | 销售金额-套餐子物品 | 套餐内金额 | DECIMAL(12,4) | 5765.5589 |
| 20 | 赠送数量-小计 | 赠送数量合计 | INT | 12 |
| 24 | 赠送金额-小计 | 赠送金额合计 | DECIMAL(12,4) | 264 |

---

### 2.3 预订汇总表结构

**文件特征**：
- 第1行：标题行（包含日期范围和门店名称）
- 第2-3行：多级表头
- 最后一行：合计行

**核心字段**（共39列）：

| 列序 | 原始字段名 | 字段含义 | 数据类型 | 示例值 |
|-----|-----------|---------|---------|--------|
| 1 | 部门 | 员工部门/角色 | VARCHAR(20) | 销售经理、收银员、服务员 |
| 2 | 订位人 | 员工姓名 | VARCHAR(50) | 申海宁、黄锦琪 |
| 3 | 订台数 | 预订包厢数 | INT | 3 |
| 4 | 销售金额 | 销售业绩 | DECIMAL(12,2) | 2700 |
| 5 | 服务费 | 服务费 | DECIMAL(12,2) | 0 |
| 6 | 自动扣减 | 自动扣减 | DECIMAL(12,2) | 798 |
| 7 | 基本业绩 | 基础业绩 | DECIMAL(12,2) | 1902 |
| 8 | 免单金额 | 免单金额 | DECIMAL(12,2) | 0 |
| 9 | 挂账金额 | 挂账金额 | DECIMAL(12,2) | 0 |
| 10 | 调整金额 | 调整金额 | DECIMAL(12,2) | 0 |
| 11 | 折扣金额 | 折扣金额 | DECIMAL(12,2) | 0 |
| 12 | 抹零金额 | 抹零金额 | DECIMAL(12,2) | 0 |
| 13 | 赠送金额 | 赠送金额 | DECIMAL(12,2) | 38 |
| 14 | 应收金额 | 应收金额 | DECIMAL(12,2) | 9194.97 |
| 15 | 实收金额 | 实收金额 | DECIMAL(12,2) | 2700 |

**支付方式字段**（第16-37列）：

| 原始字段名 | 分类 |
|-----------|------|
| 微信支付 | 收入类 |
| 支付宝 | 收入类 |
| 会员支付 | 权益类 |
| 会员本金 | 权益类-本金 |
| 会员赠送 | 权益类-赠送 |
| 现金 | 收入类 |
| pos银行卡 | 收入类 |
| 员工信用扣款 | 权益类 |
| 付呗 | 收入类 |
| 团购 | 收入类 |
| 店长签单 | 权益类 |
| 演绎提成 | 权益类 |
| 抖音 | 收入类 |
| POS机 | 收入类 |
| 营销提成 | 权益类 |
| 过期取酒 | 权益类 |
| 招待 | 权益类 |
| 会员停用 | 权益类 |
| 三倍充值活动 | 权益类 |
| 往来款 | 权益类 |
| 高德 | 收入类 |

---

## 三、技术架构设计

### 3.1 技术选型

| 层级 | 技术选择 | 版本 | 选型理由 |
|------|---------|------|---------|
| **后端语言** | Python | 3.9+ | Pandas 处理 Excel/CSV 无敌 |
| **后端框架** | FastAPI | 0.100+ | 高性能、自动生成API文档 |
| **数据处理** | Pandas | 2.0+ | 数据清洗ETL首选 |
| **数据库** | MySQL | 8.0+ | 结构化数据，支持窗口函数 |
| **ORM** | SQLAlchemy | 2.0+ | Python 数据库标准 |
| **前端框架** | Vue 3 | 3.4+ | 国内生态好，学习曲线平缓 |
| **UI组件库** | Element Plus | 2.4+ | Vue 3 配套组件库 |
| **图表库** | ECharts | 5.4+ | 国内图表标准，文档全 |

### 3.2 系统架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                        前端 (Vue 3 + ECharts)                      │
├──────────────────────────────────────────────────────────────────┤
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐  │
│  │ 数据上传中心 │  │ 综合驾驶舱  │  │ 专项分析页  │  │ 系统管理   │  │
│  │ (各店店长用)  │  │ (老板看图)  │  │ (深入分析)  │  │ (门店管理)  │  │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘  │
└────────────────────────────┬─────────────────────────────────────┘
                             │ HTTP/REST API
┌────────────────────────────┴─────────────────────────────────────┐
│                     后端 (FastAPI + Pandas)                        │
├──────────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                   智能解析器 (The Parser)                    │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │  │
│  │  │ 文件识别  │→│ 表头解析  │→│ 数据清洗  │→│ 入库存储  │   │  │
│  │  │ (特征匹配) │  │ (字段映射) │  │ (删合计行) │  │ (写事实表) │   │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │  │
│  └────────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                   数据查询 API                               │  │
│  │  GET /api/dashboard/overview     ← 总业绩、总毛利            │  │
│  │  GET /api/chart/staff-ranking    ← 员工业绩排行              │  │
│  │  GET /api/chart/room-efficiency  ← 包厢效能分析              │  │
│  │  GET /api/chart/product-sales    ← 商品销售分析              │  │
│  └────────────────────────────────────────────────────────────┘  │
└────────────────────────────┬─────────────────────────────────────┘
                             │ SQL
┌────────────────────────────┴─────────────────────────────────────┐
│                       数据库 (MySQL 8.0+)                          │
├──────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────────────────────────────────┐  │
│  │  系统管理表   │  │              业务事实表                   │  │
│  │  ┌─────────┐ │  │  ┌──────────────────────────────────────┐│  │
│  │  │sys_store│ │  │  │ fact_room_analysis (包厢开台分析)    ││  │
│  │  └─────────┘ │  │  │ fact_beverage_sales (酒水销售分析)   ││  │
│  │  ┌──────────┐│  │  │ fact_staff_kpi (人员绩效/预订汇总)   ││  │
│  │  │sys_upload││  │  └──────────────────────────────────────┘│  │
│  │  │   _log   ││  │                                          │  │
│  │  └──────────┘│  │                                          │  │
│  └──────────────┘  └──────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 四、数据库设计

### 4.1 系统管理表

#### 4.1.1 门店表 (sys_store)

```sql
CREATE TABLE sys_store (
    id INT PRIMARY KEY AUTO_INCREMENT,
    store_code VARCHAR(20) NOT NULL UNIQUE COMMENT '门店编码',
    store_name VARCHAR(100) NOT NULL COMMENT '门店名称',
    region VARCHAR(50) COMMENT '所属区域/城市',
    address VARCHAR(200) COMMENT '门店地址',
    status TINYINT DEFAULT 1 COMMENT '状态: 1-启用, 0-停用',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) COMMENT '门店信息表';

-- 初始数据
INSERT INTO sys_store (store_code, store_name, region) VALUES
('WXC', '空境·派对KTV（万象城店）', '南宁'),
('QNL', '空境·派对KTV（青年路店）', '南宁');
```

#### 4.1.2 上传日志表 (sys_upload_log)

```sql
CREATE TABLE sys_upload_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    store_id INT NOT NULL COMMENT '门店ID',
    file_name VARCHAR(255) NOT NULL COMMENT '原始文件名',
    file_type ENUM('room_analysis', 'beverage_sales', 'booking_summary') NOT NULL COMMENT '文件类型',
    file_hash VARCHAR(64) NOT NULL COMMENT '文件MD5哈希(防重复)',
    data_start_date DATE NOT NULL COMMENT '数据开始日期',
    data_end_date DATE NOT NULL COMMENT '数据结束日期',
    data_month VARCHAR(7) NOT NULL COMMENT '数据所属月份(YYYY-MM)',
    row_count INT NOT NULL COMMENT '数据行数(不含表头和合计)',
    upload_user VARCHAR(50) NOT NULL COMMENT '上传人',
    upload_status ENUM('pending', 'success', 'failed') DEFAULT 'pending' COMMENT '上传状态',
    error_message TEXT COMMENT '错误信息',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_store_month (store_id, data_month),
    INDEX idx_file_hash (file_hash),
    FOREIGN KEY (store_id) REFERENCES sys_store(id)
) COMMENT '文件上传日志表';
```

### 4.2 业务事实表

#### 4.2.1 包厢开台分析表 (fact_room_analysis)

```sql
CREATE TABLE fact_room_analysis (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    upload_log_id BIGINT NOT NULL COMMENT '关联上传日志',
    store_id INT NOT NULL COMMENT '门店ID',
    
    -- 包厢信息
    room_name VARCHAR(20) NOT NULL COMMENT '包厢名称',
    room_type VARCHAR(20) NOT NULL COMMENT '包厢类型',
    area_name VARCHAR(20) COMMENT '区域名称',
    
    -- 订单信息
    order_no VARCHAR(30) NOT NULL COMMENT '开台单号',
    billing_mode VARCHAR(100) COMMENT '开房计费模式',
    
    -- 时间信息
    open_time DATETIME NOT NULL COMMENT '开房时间',
    close_time DATETIME COMMENT '关房时间',
    clean_time DATETIME COMMENT '清洁时间',
    business_date DATE NOT NULL COMMENT '营业日',
    
    -- 人员信息
    booker VARCHAR(50) COMMENT '订房人',
    booker_dept VARCHAR(50) COMMENT '订房人部门',
    returner VARCHAR(50) COMMENT '返房人',
    rotator VARCHAR(50) COMMENT '轮房人',
    
    -- 消费信息
    duration_minutes INT COMMENT '消费时长(分钟)',
    time_slot VARCHAR(20) COMMENT '时段',
    open_tag VARCHAR(50) COMMENT '开房标签',
    booking_channel VARCHAR(50) COMMENT '预订渠道',
    bill_remark VARCHAR(200) COMMENT '账单备注',
    
    -- 金额信息
    total_amount DECIMAL(12,2) DEFAULT 0 COMMENT '账单合计',
    receivable_amount DECIMAL(12,2) DEFAULT 0 COMMENT '应收金额',
    received_amount DECIMAL(12,2) DEFAULT 0 COMMENT '实收金额',
    gift_amount DECIMAL(12,2) DEFAULT 0 COMMENT '赠送金额',
    round_off_amount DECIMAL(12,2) DEFAULT 0 COMMENT '抹零金额',
    adjust_amount DECIMAL(12,2) DEFAULT 0 COMMENT '调整金额',
    pending_amount DECIMAL(12,2) DEFAULT 0 COMMENT '挂账金额',
    free_amount DECIMAL(12,2) DEFAULT 0 COMMENT '免单金额',
    room_discount DECIMAL(12,2) DEFAULT 0 COMMENT '房费折扣金额',
    beverage_discount DECIMAL(12,2) DEFAULT 0 COMMENT '酒水折扣金额',
    
    -- 业绩信息
    base_performance DECIMAL(12,2) DEFAULT 0 COMMENT '基本业绩',
    min_consumption DECIMAL(12,2) DEFAULT 0 COMMENT '低消费',
    min_consumption_diff DECIMAL(12,2) DEFAULT 0 COMMENT '低消差额',
    counted_min_amount DECIMAL(12,2) DEFAULT 0 COMMENT '计入低消金额',
    uncounted_min_amount DECIMAL(12,2) DEFAULT 0 COMMENT '不计入低消金额',
    special_drink_amount DECIMAL(12,2) DEFAULT 0 COMMENT '特饮金额',
    
    -- 人员提成
    sales_manager VARCHAR(50) COMMENT '销售经理',
    marketing_manager VARCHAR(50) COMMENT '营销经理',
    
    -- 支付方式明细 (JSON存储便于扩展)
    payment_details JSON COMMENT '支付方式明细',
    
    -- 计算字段
    row_hash VARCHAR(64) COMMENT '行哈希(去重)',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_store_date (store_id, business_date),
    INDEX idx_room (room_name, room_type),
    INDEX idx_order (order_no),
    INDEX idx_row_hash (row_hash),
    FOREIGN KEY (upload_log_id) REFERENCES sys_upload_log(id),
    FOREIGN KEY (store_id) REFERENCES sys_store(id)
) COMMENT '包厢开台分析事实表';
```

#### 4.2.2 酒水销售分析表 (fact_beverage_sales)

```sql
CREATE TABLE fact_beverage_sales (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    upload_log_id BIGINT NOT NULL COMMENT '关联上传日志',
    store_id INT NOT NULL COMMENT '门店ID',
    data_month VARCHAR(7) NOT NULL COMMENT '数据月份',
    
    -- 商品信息
    product_name VARCHAR(100) NOT NULL COMMENT '酒水名称',
    category_name VARCHAR(50) NOT NULL COMMENT '类别名称',
    unit VARCHAR(10) COMMENT '单位',
    area VARCHAR(20) COMMENT '区域',
    
    -- 成本信息
    cost_total DECIMAL(12,4) DEFAULT 0 COMMENT '成本-小计',
    cost_sales DECIMAL(12,4) DEFAULT 0 COMMENT '成本-销售',
    cost_gift DECIMAL(12,4) DEFAULT 0 COMMENT '成本-赠送',
    
    -- 利润信息
    profit DECIMAL(12,4) DEFAULT 0 COMMENT '利润',
    profit_rate DECIMAL(8,2) DEFAULT 0 COMMENT '利润率(%)',
    
    -- 合计
    total_qty INT DEFAULT 0 COMMENT '合计-数量',
    total_amount DECIMAL(12,4) DEFAULT 0 COMMENT '合计-金额',
    
    -- 销售数量
    sales_qty_subtotal INT DEFAULT 0 COMMENT '销售数量-小计',
    sales_qty_pure INT DEFAULT 0 COMMENT '销售数量-销售',
    sales_qty_package INT DEFAULT 0 COMMENT '销售数量-套餐子物品',
    sales_qty_example INT DEFAULT 0 COMMENT '销售数量-例送子物品',
    
    -- 销售金额
    sales_amount_subtotal DECIMAL(12,4) DEFAULT 0 COMMENT '销售金额-小计',
    sales_amount_pure DECIMAL(12,4) DEFAULT 0 COMMENT '销售金额-销售',
    sales_amount_package DECIMAL(12,4) DEFAULT 0 COMMENT '销售金额-套餐子物品',
    sales_amount_example DECIMAL(12,4) DEFAULT 0 COMMENT '销售金额-例送子物品',
    
    -- 赠送数量
    gift_qty_subtotal INT DEFAULT 0 COMMENT '赠送数量-小计',
    gift_qty_pure INT DEFAULT 0 COMMENT '赠送数量-赠送',
    gift_qty_package INT DEFAULT 0 COMMENT '赠送数量-套餐子物品',
    
    -- 赠送金额
    gift_amount_subtotal DECIMAL(12,4) DEFAULT 0 COMMENT '赠送金额-小计',
    gift_amount_pure DECIMAL(12,4) DEFAULT 0 COMMENT '赠送金额-赠送',
    gift_amount_package DECIMAL(12,4) DEFAULT 0 COMMENT '赠送金额-套餐子物品',
    
    -- 计算字段
    row_hash VARCHAR(64) COMMENT '行哈希(去重)',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_store_month (store_id, data_month),
    INDEX idx_category (category_name),
    INDEX idx_product (product_name),
    INDEX idx_row_hash (row_hash),
    FOREIGN KEY (upload_log_id) REFERENCES sys_upload_log(id),
    FOREIGN KEY (store_id) REFERENCES sys_store(id)
) COMMENT '酒水销售分析事实表';
```

#### 4.2.3 人员绩效表 (fact_staff_kpi)

```sql
CREATE TABLE fact_staff_kpi (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    upload_log_id BIGINT NOT NULL COMMENT '关联上传日志',
    store_id INT NOT NULL COMMENT '门店ID',
    data_month VARCHAR(7) NOT NULL COMMENT '数据月份',
    
    -- 人员信息
    department VARCHAR(20) NOT NULL COMMENT '部门/角色',
    staff_name VARCHAR(50) NOT NULL COMMENT '订位人姓名',
    
    -- 业绩信息
    booking_count INT DEFAULT 0 COMMENT '订台数',
    sales_amount DECIMAL(12,2) DEFAULT 0 COMMENT '销售金额',
    service_fee DECIMAL(12,2) DEFAULT 0 COMMENT '服务费',
    auto_deduction DECIMAL(12,2) DEFAULT 0 COMMENT '自动扣减',
    base_performance DECIMAL(12,2) DEFAULT 0 COMMENT '基本业绩',
    free_amount DECIMAL(12,2) DEFAULT 0 COMMENT '免单金额',
    pending_amount DECIMAL(12,2) DEFAULT 0 COMMENT '挂账金额',
    adjust_amount DECIMAL(12,2) DEFAULT 0 COMMENT '调整金额',
    discount_amount DECIMAL(12,2) DEFAULT 0 COMMENT '折扣金额',
    round_off_amount DECIMAL(12,2) DEFAULT 0 COMMENT '抹零金额',
    gift_amount DECIMAL(12,2) DEFAULT 0 COMMENT '赠送金额',
    receivable_amount DECIMAL(12,2) DEFAULT 0 COMMENT '应收金额',
    received_amount DECIMAL(12,2) DEFAULT 0 COMMENT '实收金额',
    
    -- 支付方式明细 (JSON存储)
    payment_details JSON COMMENT '支付方式明细',
    
    -- 酒水类别金额
    beverage_category_amount DECIMAL(12,2) DEFAULT 0 COMMENT '酒水类别金额-小计',
    
    -- 计算字段
    row_hash VARCHAR(64) COMMENT '行哈希(去重)',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_store_month (store_id, data_month),
    INDEX idx_staff (staff_name, department),
    INDEX idx_row_hash (row_hash),
    FOREIGN KEY (upload_log_id) REFERENCES sys_upload_log(id),
    FOREIGN KEY (store_id) REFERENCES sys_store(id)
) COMMENT '人员绩效事实表';
```

---

## 五、ETL 数据管道设计

### 5.1 智能解析器流程

```
┌─────────────────────────────────────────────────────────────────┐
│                      智能解析器 (The Parser)                      │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: 文件类型识别                                              │
├─────────────────────────────────────────────────────────────────┤
│ 读取第1-3行，检测特征字段:                                        │
│   • 包含"包厢名称" + "开台单号" → 包厢开台分析表                   │
│   • 包含"酒水名称" + "类别名称" → 酒水销售分析表                   │
│   • 包含"订位人" + "订台数"    → 预订汇总表                       │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: 元数据提取                                               │
├─────────────────────────────────────────────────────────────────┤
│ 从标题行提取:                                                     │
│   • 日期范围: "2025-12-01 10:00:00至2025-12-03 10:00:00"         │
│   • 门店名称: "空境·派对KTV（万象城店）"                          │
│   • 数据月份: 自动计算 → "2025-12"                               │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: 数据清洗                                                 │
├─────────────────────────────────────────────────────────────────┤
│   • 删除标题行 (第1行)                                           │
│   • 合并多级表头 (第2-3行合并为单行)                              │
│   • 删除合计行 (最后一行，首列为"合计")                           │
│   • 删除空行                                                     │
│   • 数字类型转换 (空值→0, 字符串→数字)                           │
│   • 日期时间标准化                                               │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 4: 字段映射                                                 │
├─────────────────────────────────────────────────────────────────┤
│ 将原始列名映射为数据库字段名:                                     │
│   • "包厢名称" → room_name                                       │
│   • "账单合计" → total_amount                                    │
│   • "微信支付" → payment_wechat                                  │
│   • ...                                                          │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 5: 数据校验                                                 │
├─────────────────────────────────────────────────────────────────┤
│   • 必填字段检查 (订单号、日期等)                                 │
│   • 金额平衡校验 (实收 ≈ 各支付方式之和)                          │
│   • 异常值检测 (负数金额、时间交叉等)                             │
│   • 文件哈希计算 (防重复上传)                                    │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│ Step 6: 入库存储                                                 │
├─────────────────────────────────────────────────────────────────┤
│   • 创建上传日志记录                                             │
│   • 批量插入事实表                                               │
│   • 更新上传状态为成功                                           │
│   • 失败时回滚并记录错误                                         │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 核心解析代码示例

```python
# backend/app/core/parsing/file_identifier.py

import pandas as pd
import re
from enum import Enum
from typing import Tuple, Optional
from datetime import datetime

class FileType(Enum):
    ROOM_ANALYSIS = "room_analysis"           # 包厢开台分析
    BEVERAGE_SALES = "beverage_sales"         # 酒水销售分析
    BOOKING_SUMMARY = "booking_summary"       # 预订汇总
    UNKNOWN = "unknown"

class FileIdentifier:
    """文件类型智能识别器"""
    
    # 特征字段定义
    FEATURE_PATTERNS = {
        FileType.ROOM_ANALYSIS: ["包厢名称", "开台单号", "开房时间", "关房时间"],
        FileType.BEVERAGE_SALES: ["酒水名称", "类别名称", "利润", "利润率"],
        FileType.BOOKING_SUMMARY: ["订位人", "订台数", "销售金额", "部门"],
    }
    
    def identify(self, df: pd.DataFrame) -> FileType:
        """识别文件类型"""
        # 获取前3行所有单元格文本
        sample_text = " ".join(df.head(3).astype(str).values.flatten())
        
        for file_type, features in self.FEATURE_PATTERNS.items():
            match_count = sum(1 for f in features if f in sample_text)
            if match_count >= 2:  # 至少匹配2个特征
                return file_type
        
        return FileType.UNKNOWN
    
    def extract_metadata(self, df: pd.DataFrame) -> dict:
        """从标题行提取元数据"""
        title_row = str(df.iloc[0, 0]) if len(df) > 0 else ""
        
        # 提取日期范围: "2025-12-01 10:00:00至2025-12-03 10:00:00"
        date_pattern = r"(\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}:\d{2})至(\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}:\d{2})"
        date_match = re.search(date_pattern, title_row)
        
        # 提取门店名称: "空境·派对KTV（万象城店）"
        store_pattern = r"[（(]([^）)]+)[）)]"
        store_matches = re.findall(store_pattern, title_row)
        store_name = store_matches[-1] if store_matches else ""
        
        metadata = {
            "start_date": None,
            "end_date": None,
            "data_month": None,
            "store_name": store_name,
        }
        
        if date_match:
            start_dt = datetime.strptime(date_match.group(1), "%Y-%m-%d %H:%M:%S")
            end_dt = datetime.strptime(date_match.group(2), "%Y-%m-%d %H:%M:%S")
            metadata["start_date"] = start_dt.date()
            metadata["end_date"] = end_dt.date()
            metadata["data_month"] = start_dt.strftime("%Y-%m")
        
        return metadata
```

### 5.3 数据清洗代码示例

```python
# backend/app/core/cleaning/data_cleaner.py

import pandas as pd
import numpy as np
from typing import Tuple

class DataCleaner:
    """数据清洗器"""
    
    def clean_room_analysis(self, df: pd.DataFrame) -> pd.DataFrame:
        """清洗包厢开台分析表"""
        # 1. 删除标题行(第1行)
        df = df.iloc[1:].reset_index(drop=True)
        
        # 2. 设置表头 (第1行为主表头，第2行为支付方式子表头)
        main_header = df.iloc[0].fillna('')
        sub_header = df.iloc[1].fillna('')
        
        # 合并表头: 支付方式部分需要合并
        new_columns = []
        for i, (m, s) in enumerate(zip(main_header, sub_header)):
            if m == '' and s != '':
                new_columns.append(s)
            elif m != '' and s == '':
                new_columns.append(m)
            else:
                new_columns.append(m)
        
        df.columns = new_columns
        df = df.iloc[2:].reset_index(drop=True)
        
        # 3. 删除合计行 (首列为"合计")
        df = df[df.iloc[:, 0] != '合计'].reset_index(drop=True)
        
        # 4. 删除空行
        df = df.dropna(how='all').reset_index(drop=True)
        
        # 5. 数字类型转换
        numeric_columns = [
            '消费时长', '账单合计', '应收金额', '实收金额', '赠送', '抹零金额',
            '基本业绩', '低消费', '低消差额', '计入低消金额',
            '微信支付', '支付宝', '会员支付', '会员本金', '会员赠送',
            '现金', '银行卡', '付呗', '美团', '抖音', '高德'
        ]
        for col in numeric_columns:
            if col in df.columns:
                df[col] = pd.to_numeric(df[col], errors='coerce').fillna(0)
        
        # 6. 日期时间标准化
        datetime_columns = ['开房时间', '关房时间', '清洁时间']
        for col in datetime_columns:
            if col in df.columns:
                df[col] = pd.to_datetime(df[col], errors='coerce')
        
        date_columns = ['营业日']
        for col in date_columns:
            if col in df.columns:
                df[col] = pd.to_datetime(df[col], errors='coerce').dt.date
        
        return df
    
    def clean_beverage_sales(self, df: pd.DataFrame) -> pd.DataFrame:
        """清洗酒水销售分析表"""
        # 1. 删除标题行
        df = df.iloc[1:].reset_index(drop=True)
        
        # 2. 合并多级表头 (第1行主表头，第2行子表头)
        # 酒水销售表的表头结构: 成本(小计/销售/赠送), 销售数量(小计/销售/套餐子物品)等
        main_header = df.iloc[0].fillna('')
        sub_header = df.iloc[1].fillna('')
        
        new_columns = []
        current_main = ''
        for i, (m, s) in enumerate(zip(main_header, sub_header)):
            if m != '':
                current_main = m
            if s != '':
                new_columns.append(f"{current_main}_{s}" if current_main else s)
            else:
                new_columns.append(m if m else f"col_{i}")
        
        df.columns = new_columns
        df = df.iloc[2:].reset_index(drop=True)
        
        # 3. 删除合计行
        df = df[df.iloc[:, 0] != '合计'].reset_index(drop=True)
        
        # 4. 删除空行
        df = df.dropna(how='all').reset_index(drop=True)
        
        # 5. 数字类型转换
        exclude_cols = ['酒水名称', '类别名称', '单位', '区域']
        for col in df.columns:
            if col not in exclude_cols:
                df[col] = pd.to_numeric(df[col], errors='coerce').fillna(0)
        
        return df
    
    def clean_booking_summary(self, df: pd.DataFrame) -> pd.DataFrame:
        """清洗预订汇总表"""
        # 与包厢开台分析类似的处理逻辑
        df = df.iloc[1:].reset_index(drop=True)
        
        main_header = df.iloc[0].fillna('')
        sub_header = df.iloc[1].fillna('')
        
        new_columns = []
        for i, (m, s) in enumerate(zip(main_header, sub_header)):
            if m == '' and s != '':
                new_columns.append(s)
            elif m != '' and s == '':
                new_columns.append(m)
            else:
                new_columns.append(m)
        
        df.columns = new_columns
        df = df.iloc[2:].reset_index(drop=True)
        
        # 删除合计行
        df = df[df.iloc[:, 0] != '合计'].reset_index(drop=True)
        
        # 删除空行
        df = df.dropna(how='all').reset_index(drop=True)
        
        # 数字类型转换
        exclude_cols = ['部门', '订位人']
        for col in df.columns:
            if col not in exclude_cols:
                df[col] = pd.to_numeric(df[col], errors='coerce').fillna(0)
        
        return df
```

---

## 六、API 接口设计

### 6.1 接口总览

| 模块 | 接口 | 方法 | 说明 |
|------|------|------|------|
| **文件上传** | `/api/upload/preview` | POST | 上传文件预览解析结果 |
| | `/api/upload/confirm` | POST | 确认入库 |
| | `/api/upload/history` | GET | 上传历史列表 |
| **仪表盘** | `/api/dashboard/overview` | GET | 总览KPI数据 |
| | `/api/dashboard/trend` | GET | 业绩趋势数据 |
| **图表数据** | `/api/chart/staff-ranking` | GET | 员工业绩排行 |
| | `/api/chart/room-efficiency` | GET | 包厢效能分析 |
| | `/api/chart/product-sales` | GET | 商品销售分析 |
| | `/api/chart/payment-structure` | GET | 支付结构分析 |
| **门店管理** | `/api/stores` | GET | 门店列表 |

### 6.2 接口详细设计

#### 6.2.1 文件上传预览

```
POST /api/upload/preview
Content-Type: multipart/form-data

Request:
  - file: 上传的CSV/Excel文件
  
Response (200):
{
    "success": true,
    "data": {
        "file_type": "room_analysis",
        "file_type_name": "包厢开台分析表",
        "store_name": "万象城店",
        "store_id": 1,
        "data_month": "2025-12",
        "date_range": {
            "start": "2025-12-01",
            "end": "2025-12-03"
        },
        "row_count": 78,
        "preview_rows": [
            {
                "room_name": "K07",
                "room_type": "电音中包",
                "order_no": "Z-KT25120200041",
                "open_time": "2025-12-03 00:37:29",
                "total_amount": 225.00
            }
            // ... 前5行预览
        ],
        "validation": {
            "is_valid": true,
            "warnings": [],
            "errors": []
        },
        "is_duplicate": false,
        "session_id": "uuid-xxx"  // 用于确认入库
    }
}

Response (400 - 格式错误):
{
    "success": false,
    "error": {
        "code": "INVALID_FORMAT",
        "message": "文件格式不符，缺少必要字段：开台单号"
    }
}
```

#### 6.2.2 确认入库

```
POST /api/upload/confirm
Content-Type: application/json

Request:
{
    "session_id": "uuid-xxx",
    "upload_user": "各店店长"
}

Response (200):
{
    "success": true,
    "data": {
        "upload_log_id": 123,
        "imported_rows": 78,
        "message": "成功导入 78 条包厢开台数据"
    }
}
```

#### 6.2.3 仪表盘总览

```
GET /api/dashboard/overview?month=2025-12&store_id=1

Response (200):
{
    "success": true,
    "data": {
        "period": "2025-12",
        "store": {
            "id": 1,
            "name": "万象城店"
        },
        "kpi": {
            "total_revenue": {
                "value": 221988.93,
                "unit": "元",
                "change_rate": 12.5,  // 环比增长率
                "trend": "up"
            },
            "total_profit": {
                "value": 11282.92,
                "unit": "元",
                "change_rate": 8.3,
                "trend": "up"
            },
            "room_count": {
                "value": 255,
                "unit": "台",
                "change_rate": -3.2,
                "trend": "down"
            },
            "avg_consumption": {
                "value": 870.54,
                "unit": "元/台",
                "change_rate": 5.1,
                "trend": "up"
            }
        }
    }
}
```

#### 6.2.4 员工业绩排行

```
GET /api/chart/staff-ranking?month=2025-12&store_id=1&limit=10

Response (200):
{
    "success": true,
    "data": {
        "chart_type": "bar",
        "title": "员工业绩TOP10",
        "categories": ["常含", "张伟青年路", "姚杰", "饶慧", "小宗", ...],
        "series": [
            {
                "name": "销售金额",
                "data": [90889, 35509.93, 17850, 15161, 18043, ...]
            },
            {
                "name": "基本业绩",
                "data": [66934, 20246.93, 12106, 8037, 16667, ...]
            }
        ]
    }
}
```

#### 6.2.5 商品销售分析

```
GET /api/chart/product-sales?month=2025-12&store_id=1&category=啤酒

Response (200):
{
    "success": true,
    "data": {
        "summary": {
            "total_sales_amount": 6029.56,
            "total_cost": 746.67,
            "total_profit": 5018.89,
            "avg_profit_rate": 672.17
        },
        "by_product": [
            {
                "product_name": "青岛崂山",
                "category": "啤酒",
                "sales_qty": 308,
                "sales_amount": 5765.56,
                "profit": 5018.89,
                "profit_rate": 672.17
            },
            {
                "product_name": "蓝带拉环",
                "category": "啤酒",
                "sales_qty": 12,
                "sales_amount": 280.00,
                "profit": 235.39,
                "profit_rate": 527.68
            }
            // ...
        ]
    }
}
```

---

## 七、前端页面设计

### 7.1 页面结构

```
┌──────────────────────────────────────────────────────────────────┐
│                         系统导航栏                                │
│  [Logo] KTV经营分析系统    [数据上传] [综合驾驶舱] [专项分析]  [设置] │
└──────────────────────────────────────────────────────────────────┘

页面1: 数据上传中心 (/upload)
页面2: 综合驾驶舱 (/dashboard)  
页面3: 专项分析 (/analysis)
  - 3.1 人员风云榜 (/analysis/staff)
  - 3.2 商品销售 (/analysis/products)
  - 3.3 包厢效能 (/analysis/rooms)
  - 3.4 门店对比 (/analysis/compare)
```

### 7.2 页面1: 数据上传中心

**用户**: 数据管理员

**功能设计**:

```
┌──────────────────────────────────────────────────────────────────┐
│                       数据上传中心                                │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                                                             │ │
│  │          📁 点击或拖拽文件到此处上传                         │ │
│  │                                                             │ │
│  │             支持 .csv .xls .xlsx 格式                       │ │
│  │                                                             │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ 解析结果预览                                                 │ │
│  ├─────────────────────────────────────────────────────────────┤ │
│  │ 📊 识别为: 包厢开台分析表                                    │ │
│  │ 🏪 门店: 万象城店                                           │ │
│  │ 📅 数据月份: 2025年12月                                     │ │
│  │ 📝 数据行数: 78 条                                          │ │
│  │ ✅ 校验结果: 通过                                           │ │
│  │                                                             │ │
│  │ 数据预览:                                                   │ │
│  │ ┌─────────┬──────────┬──────────────┬────────────┐         │ │
│  │ │ 包厢名称 │ 包厢类型  │ 开台单号      │ 账单合计   │         │ │
│  │ ├─────────┼──────────┼──────────────┼────────────┤         │ │
│  │ │ K07     │ 电音中包  │ Z-KT25...041 │ ¥225.00   │         │ │
│  │ │ K11     │ 电音小包  │ Z-KT25...040 │ ¥193.00   │         │ │
│  │ │ ...     │ ...      │ ...          │ ...       │         │ │
│  │ └─────────┴──────────┴──────────────┴────────────┘         │ │
│  │                                                             │ │
│  │            [取消]  [确认入库]                                │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ 最近上传记录                                                 │ │
│  ├─────────────────────────────────────────────────────────────┤ │
│  │ 2025-12-08 14:30  万象城店  包厢开台分析  78条  成功         │ │
│  │ 2025-12-08 14:28  万象城店  酒水销售分析  34条  成功         │ │
│  │ 2025-12-08 14:25  青年路店  预订汇总      16条  成功         │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

### 7.3 页面2: 综合驾驶舱

**用户**: 老板

**功能设计**:

```
┌──────────────────────────────────────────────────────────────────┐
│ 综合驾驶舱                     门店: [万象城店 ▼]  月份: [2025-12 ▼] │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────┐ │
│  │ 💰 本月营收   │ │ 📈 环比增长   │ │ 💎 毛利率    │ │ 🎤 开台数 │ │
│  │              │ │              │ │              │ │          │ │
│  │  ¥221,989   │ │   +12.5%    │ │   42.5%     │ │   255   │ │
│  │  ↑5.2%      │ │              │ │   ↑2.1%     │ │   ↓3.2% │ │
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────────┘ │
│                                                                   │
│  ┌────────────────────────────────┐ ┌────────────────────────────┐│
│  │ 📈 业绩趋势 (最近6个月)         │ │ 🥧 收入构成                 ││
│  │                                │ │                            ││
│  │     ╭─────────────────╮       │ │      ┌─────────┐           ││
│  │  20万├                 │       │ │     ╱    酒水   ╲          ││
│  │     │    ╭───╮        │       │ │    │   45%    │          ││
│  │  15万├───╯   ╰───╮    │       │ │    │          │  房费     ││
│  │     │           ╰────│       │ │    ╲   30%   ╱  25%      ││
│  │  10万├                │       │ │     ╲       ╱            ││
│  │     └─────────────────┘       │ │      ╲─────╱             ││
│  │      7月 8月 9月 10月 11月 12月 │ │                            ││
│  └────────────────────────────────┘ └────────────────────────────┘│
│                                                                   │
│  ┌────────────────────────────────┐ ┌────────────────────────────┐│
│  │ 👑 员工业绩TOP5                 │ │ 🍺 热销商品TOP5             ││
│  │                                │ │                            ││
│  │  常含        ████████████ 90K │ │  青岛崂山    ████████ 320瓶 ││
│  │  张伟        ████████     35K │ │  百岁山      ██████   27瓶  ││
│  │  姚杰        ██████       18K │ │  喜力铝罐    ████     3瓶   ││
│  │  小宗        ██████       18K │ │  什锦果盘    ███      9份   ││
│  │  饶慧        █████        15K │ │  卤水花生    ██       10份  ││
│  └────────────────────────────────┘ └────────────────────────────┘│
└──────────────────────────────────────────────────────────────────┘
```

### 7.4 页面3: 专项分析

#### 3.1 人员风云榜

```
┌──────────────────────────────────────────────────────────────────┐
│ 人员风云榜                      [销售经理] [服务员] [收银员] [全部] │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                     业绩排名柱状图                          │  │
│  │                                                            │  │
│  │  常含     ████████████████████████████████████████  90,889 │  │
│  │  张伟     ██████████████████                        35,510 │  │
│  │  姚杰     ███████████                               17,850 │  │
│  │  小宗     ███████████                               18,043 │  │
│  │  饶慧     █████████                                 15,161 │  │
│  │  黄怡芳   █████                                      9,309 │  │
│  │  王定胜   ██                                         3,302 │  │
│  │  ...                                                       │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ 详细数据表格                                                │  │
│  ├────────┬────────┬────────┬────────┬────────┬────────┬─────┤  │
│  │ 排名   │ 姓名    │ 部门   │ 订台数  │ 销售金额 │ 基本业绩 │ 实收 │  │
│  ├────────┼────────┼────────┼────────┼────────┼────────┼─────┤  │
│  │ 1     │ 常含    │ 收银员  │ 93     │ 90,889 │ 66,934 │90,889│  │
│  │ 2     │ 张伟    │ 服务员  │ 36     │ 35,510 │ 20,247 │35,510│  │
│  │ ...   │ ...    │ ...    │ ...    │ ...    │ ...    │ ... │  │
│  └────────┴────────┴────────┴────────┴────────┴────────┴─────┘  │
└──────────────────────────────────────────────────────────────────┘
```

#### 3.2 滞销商品预警

```
┌──────────────────────────────────────────────────────────────────┐
│ 滞销商品预警                              筛选条件: 近30天销量=0    │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ⚠️ 以下商品在过去30天内销量为0，建议关注:                          │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │ 商品名称        │ 类别     │ 库存 │ 最后销售日期 │ 建议操作    ││
│  ├─────────────────┼──────────┼──────┼─────────────┼────────────┤│
│  │ 马爹利XO       │ 洋酒     │ 12瓶 │ 2025-10-15  │ 促销/下架  ││
│  │ 蓝带啤酒(大瓶) │ 啤酒     │ 24瓶 │ 2025-10-20  │ 促销       ││
│  │ 进口红酒A      │ 红酒     │ 6瓶  │ 2025-09-30  │ 下架       ││
│  └──────────────────────────────────────────────────────────────┘│
└──────────────────────────────────────────────────────────────────┘
```

---

## 八、开发计划与里程碑

### 8.1 整体时间规划

| 阶段 | 时间 | 主要任务 | 交付物 |
|------|------|---------|--------|
| **第一阶段** | Week 1-2 | 项目初始化、数据库设计 | 数据库脚本、项目骨架 |
| **第二阶段** | Week 3-4 | ETL数据管道开发 | 文件解析器、数据清洗器 |
| **第三阶段** | Week 5-6 | 后端API开发 | 上传接口、查询接口 |
| **第四阶段** | Week 7-8 | 前端开发 | 上传页面、驾驶舱页面 |
| **第五阶段** | Week 9-10 | 联调测试、优化 | 完整可用系统 |

### 8.2 详细任务分解

#### 第一阶段 (Week 1-2)

- [ ] 项目目录结构初始化
- [ ] 后端 FastAPI 项目搭建
- [ ] 前端 Vue 3 项目搭建
- [ ] MySQL 数据库创建
- [ ] 数据库表结构设计和创建
- [ ] 初始化门店数据

#### 第二阶段 (Week 3-4)

- [ ] 文件类型识别器 (`FileIdentifier`)
- [ ] 元数据提取器 (`MetadataExtractor`)
- [ ] 包厢开台分析表清洗器
- [ ] 酒水销售分析表清洗器
- [ ] 预订汇总表清洗器
- [ ] 数据校验器 (`DataValidator`)
- [ ] 单元测试编写

#### 第三阶段 (Week 5-6)

- [ ] 文件上传预览接口
- [ ] 确认入库接口
- [ ] 上传历史接口
- [ ] 仪表盘总览接口
- [ ] 员工排行接口
- [ ] 商品销售接口
- [ ] 接口文档完善

#### 第四阶段 (Week 7-8)

- [ ] 通用布局组件
- [ ] 文件上传组件
- [ ] 数据上传中心页面
- [ ] 综合驾驶舱页面
- [ ] ECharts 图表组件封装
- [ ] 专项分析页面

#### 第五阶段 (Week 9-10)

- [ ] 前后端联调
- [ ] 用真实数据测试
- [ ] Bug 修复
- [ ] 性能优化
- [ ] 部署上线

---

## 九、风险与应对

### 9.1 技术风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|---------|
| Excel格式变化 | 解析失败 | 中 | 支持多版本格式，增加容错处理 |
| 数据量大导致慢 | 用户体验差 | 低 | 分页加载、异步处理、缓存 |
| 支付方式变化 | 统计不准 | 中 | 配置化管理，支持动态扩展 |

### 9.2 业务风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|---------|
| 口径定义不清 | 数据不一致 | 高 | 与业务方确认，文档化口径 |
| 重复上传 | 数据重复 | 中 | 文件哈希校验，提示用户 |
| 跨月数据 | 统计混乱 | 低 | 按营业日统计，非自然日 |

---

## 十、附录

### 10.1 字段映射表

详见各表结构设计中的字段定义。

### 10.2 支付方式分类

| 分类 | 支付方式 | 统计口径 |
|------|---------|---------|
| **收入类** | 微信支付、支付宝、现金、银行卡、付呗、美团、抖音、高德、定金消费 | 计入实收 |
| **权益类** | 会员支付、店长签单、招待、员工信用扣款、营销提成、演绎提成、过期取酒 | 不计入实收 |

### 10.3 时段定义

| 时段 | 时间范围 | 说明 |
|------|---------|------|
| 白天档 | 10:00 - 18:00 | 日常时段 |
| 黄金档 | 18:00 - 04:30 | 高峰时段 |
| 整档 | 全天 | 特殊套餐 |

---

**文档结束**

