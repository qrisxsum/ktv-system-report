# ========================================
# KTV 报表系统 - 生产环境配置示例
# ========================================
# 复制此文件为 .env.prod 并修改配置
# cp env.prod.example .env.prod
#
# ⚠️ 重要：部署到生产环境前，请务必修改所有密码和密钥！
#
# 部署说明：
#   - 有域名：使用 scripts/nginx-prod.conf.example 配置 Nginx
#   - 无域名：使用 nginx-prod-no-domain.conf.example（根目录）配置 Nginx
#   - 详细部署步骤请参考：docs/公网部署指南.md
# ========================================

# MySQL 数据库配置
# ⚠️ 请修改为强密码！建议使用 openssl rand -base64 32 生成
MYSQL_ROOT_PASSWORD=your_strong_root_password_here
MYSQL_DATABASE=ktv_report
MYSQL_USER=ktv_user
MYSQL_PASSWORD=your_strong_db_password_here

# 后端配置
# ⚠️ 请修改为强密钥！建议使用以下命令生成：
# python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your_jwt_secret_key_here_change_in_production
SECRET_KEY=your_secret_key_here_change_in_production

# 端口配置（通常不需要修改，因为使用 Nginx 反向代理）
# BACKEND_PORT=8000
# FRONTEND_PORT=8080

